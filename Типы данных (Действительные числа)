#include <iostream>
#include <iomanip> // Для управления выводом чисел с плавающей точкой
using namespace std;

int main() {
    double a, b, c;
    cin >> a >> b >> c;

    if (a + b == c) {
        cout << "YES" << endl;
    } else {
        cout << "NO" << endl;
    }

    return 0;
}
